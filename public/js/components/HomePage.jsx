import React, {PropTypes} from 'react';
import {Link,IndexLink} from 'react-router';
import Auth from '../modules/Auth';

const HomePage = () => (
    <div>
        <h1>This is the beautiful homepage!</h1>
    </div>
);

export default HomePage;